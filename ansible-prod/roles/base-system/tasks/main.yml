---
- name: Install base system packages
  ansible.builtin.apt:
    pkg:
      - ca-certificates
      - curl
      - dos2unix
      - gnupg
      - openssh-server
      - rename
      - tree
      - git 
      - rsync 
      - unzip 
      - zip 
